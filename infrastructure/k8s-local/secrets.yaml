# Local secrets manifest — values are base64-encoded.
# DO NOT commit real credentials. Fill in your values below before applying.
#
# To generate a base64 value:       echo -n 'your-value-here' | base64
# To decode and verify:             echo 'base64string' | base64 -d
#
# database_url: postgres://postgres:rootPass1@host-postgres:5432/my_hn?sslmode=disable
apiVersion: v1
kind: Secret
metadata:
  name: secrets
type: Opaque
data:
  # postgres://postgres:rootPass1@host-postgres:5432/my_hn?sslmode=disable
  database_url: cG9zdGdyZXM6Ly9wb3N0Z3Jlczpyb290UGFzczFAaG9zdC1wb3N0Z3Jlczo1NDMyL215X2huP3NzbG1vZGU9ZGlzYWJsZQ==
  # rootPass1
  password: cm9vdFBhc3Mx
  # Fill in your Google OAuth credentials (base64-encoded):
  google_client_id: REPLACE_WITH_BASE64_GOOGLE_CLIENT_ID
  google_client_secret: REPLACE_WITH_BASE64_GOOGLE_CLIENT_SECRET
  # Fill in a JWT signing secret (base64-encoded, any random string):
  jwt_secret: REPLACE_WITH_BASE64_JWT_SECRET
